*Part of the [migration log](../../DILIGENT_MIGRATION_LOG.md). History, not instructions — read it before working in this area, not otherwise. Anything here that must not be re-broken belongs in [DILIGENT_MIGRATION.md](../../DILIGENT_MIGRATION.md)'s Invariants instead.*

### Phase 7.b — the skybox

Three decisions to the project owner up front, all taken as recommended, and the second one is the one that shaped the work: a Cubemap skybox asset is an **equirectangular image**, not a DDS/KTX cube. That looks like the more expensive option and is not — the procedural sky already needs a pass that fills six cube faces from a direction, so unwrapping an equirect is the same pass with a different source, while a DDS cube would have been a second, shorter path plus an authoring step nobody wants before every import.

**The dataset is the model.** Hosek-Wilkie without its coefficient tables is nothing, so the official v1.4a RGB dataset was fetched into `rendering/sky/hosekWilkieData.inl` (65 KB) rather than reconstructed. Its licence is BSD-3-clause and the authentic text was pulled from a mirror of `ArHosekSkyModel.h` instead of written from memory — the first thing found, an MIT file in the wrapper repository, turned out to be that repository's own licence and not the data's, and its `COPYING` was a 404. The attribution obligation is now in the plan, because nothing in the build will ever raise it.

The split is the whole reason the model is affordable: nine coefficients per channel depend only on turbidity, ground albedo and sun elevation, and everything else depends only on the direction. `cookHosekWilkieSky` does the first on the CPU, once per rebake — a quintic Bezier in elevation, then linear between the two tabulated turbidity steps and the two albedo extremes — and `skyProcedural.psh` does the second per texel. Thirty floats cross the boundary.

**Four defects, in the order they surfaced, and only the first was a real coding mistake.**

- **`pow(vec3, float)` does not exist in GLSL.** HLSL promotes the scalar; the converter does not. Caught at first launch by the runtime shader log, which is the only reason it was a two-minute fix rather than a black screen to stare at. The shader invariant now names it.
- **The sky came out upside down** — dark above, pale below. This is the cube-face table again, and this time it was derived rather than guessed: the sampling rule for +X is `s = -z, t = -y`, so v runs *down* along −Y, and on GL v increases with NDC y. All six of my up axes were inverted. The derivation landed on exactly the table the omni shadow cube already uses, which is worth noticing — two independent routes to the same six vectors. The span is now taken through `NormalizedDeviceXYToTexUV`, which makes the pass correct on D3D too rather than only on the API it was debugged on.
- **Then the sky went black, and the shader was innocent.** `SkyboxProceduralParameters` was rebuilt around the model, and two field *names* survived that rebuild with different meanings: `skyEnergy` and `sunSize`. Every saved config held `0` for both, left over from the gradient sky. `InspectorStruct::deserialize` skips a name it does not find, which is what makes adding fields safe — and is exactly why a name that *stays* is dangerous. The fix was to delete the stale block from the config; the rule is in Invariants.
- **Last, the sky and the lights turned out to be on incompatible scales**, which is not a bug in either. Hosek-Wilkie returns absolute radiance and a `Light`'s intensity is an authored multiplier. At exposure 0.05 the sky was a correct blue dome and the scene was nearly black; at exposure 1 the scene was right and the sky was solid white. Only one side can move: rescaling the lights would silently invalidate every intensity authored in every scene, so `SKY_RADIANCE_SCALE` converts the model's output where it crosses into the engine.

**Verified by pixel measurement at default settings** — Linear tone mapping, exposure 1, nothing authored. A vertical scan from zenith to horizon reads `186,220,255` → `210,238,255`, blue with B highest throughout and paling downward; a horizontal scan brightens smoothly toward the sun's azimuth, `219,249,255` on the sun side against `181,212,241` away from it. Both are what the model is supposed to produce, and neither would survive a wrong face table: a mirrored face breaks the gradient's monotonicity, which is why the scan is along it rather than a look at the picture. The scene under it still lights and shadows normally.

⚠️ **The equirectangular path has never had an image through it.** It compiles, its pipeline is built and its sampler and projection are written, but there is no HDRI in the project, so it has not been run. It is the one piece of 7.b that has not met this plan's own standard of proof.
