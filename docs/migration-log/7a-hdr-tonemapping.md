*Part of the [migration log](../../DILIGENT_MIGRATION_LOG.md). History, not instructions — read it before working in this area, not otherwise. Anything here that must not be re-broken belongs in [DILIGENT_MIGRATION.md](../../DILIGENT_MIGRATION.md)'s Invariants instead.*

### Phase 7.a — HDR, tone mapping, colour grading

Phase 7 opened with four decisions to the project owner, all before code. Three took the recommendation; one did not, and that one is the consequential one: **the procedural sky becomes Hosek-Wilkie**, not the Godot-style gradient the existing `SkyboxProceduralParameters` describes. That is only buildable because the owner separately **lifted the append-only serialization rule for `modules/engine/configs/light/` for the whole of Phase 7** — the block gets restructured, not extended. The other three: 7.a before the skybox, engine-owned tone mapping operators over DiligentFX's `TONE_MAPPING_MODE` macro, and SSAO **and** SSR in scope rather than SSAO alone.

**What the phase is really about is one target becoming three.** The scene had been shading into `RGBA8_UNORM` and encoding with a `pow(color, 1/2.2)` on the last line of `scene.psh` — which means nothing brighter than white ever existed, so there was nothing for a tone mapper or a bloom threshold to operate on. Now: `_sceneColor` is `RGBA16_FLOAT` and holds linear radiance, `_sceneDepth` is unchanged, and a new `_sceneOutput` is the `RGBA8` image everything downstream sees. One fullscreen triangle resolves the first into the third.

**The rlgl overlay is what decided where the composite sits.** It draws the editor's grid and gizmos into Diligent's own attachments so scene geometry occludes them, and those colours are already encoded — running them through a tone mapper would be a second encode. So the composite runs at the bottom of `endScene`, the overlay framebuffer's colour attachment moved to `_sceneOutput`, and its depth attachment stayed on `_sceneDepth`. `rlUnloadFramebuffer` only deletes what it finds on the depth attachment (`rlgl.h:3929`), so putting a different texture on the colour attachment needed no new detach.

**The background clear is the one place the colour space actually bites.** A `Color` is authored in the space a screen shows, and it used to be written straight into an already-encoded target, so it appeared exactly as picked. Written into a linear target it has to be decoded first — and by the *inverse of the composite's own exponent*, not by the piecewise sRGB curve, or the round trip is off by a fraction of a step. `toSceneLinear` does that, which also makes `OutputColorSpace::Linear` an identity in both directions with no special case. Ambient and light colours were deliberately left alone: the scene shader already encoded on output, so those were already being treated as linear, and re-deciding that is a visual call rather than plumbing.

**Five operators, one pipeline.** `TonemapMode` is serialized and runtime-selected while DiligentFX's `ToneMapping.fxh` selects with a compile-time macro and has no ACES at all, so the operators are the engine's: passthrough, luminance-space Reinhard with a white point, the Uncharted 2 filmic curve normalized at the white point, Narkowicz's ACES fit, and AgX in the minimal published form. AgX is the one borrowed nearly verbatim from `ToneMapping.fxh`, including its output transform — its curve leaves the result display-encoded, so the outset undoes that before the pass applies its own encode. `MatrixFromRows` is used for both AgX matrices because it is injected by both the HLSL and the GLSL definition headers and so means the same thing on either path.

Two smaller shapes worth knowing:

- **`g_SceneColor` is a DYNAMIC shader variable, not MUTABLE.** The view behind it is recreated on every viewport resize, and a mutable variable cannot be re-pointed once set — the same rule the material SRB is built around. Dynamic is set once per frame in `composite()` and needs no resize hook at all.
- **The composite pass reads its input with a point/clamp sampler**, set on the scene colour view rather than left to the backend's default. The read is one texel to one pixel, so point sampling is the exact read and a filtered default would soften the whole image by half a texel.

**The one defect, and it was a two-for-one.** The owner confirmed the scene was pixel-for-pixel unchanged, then reported that clicking any node in the node tree blanked the editor: the whole screen white, nothing drawn but the lines from `UiInspector::draw` and `NodeTreeWindow::drawLines`. First question was whether 7.a caused it, and that was answered rather than argued — the pre-7.a renderer was rebuilt into the second build directory from the same tree, and **it broke identically**. So 7.a was clean and this was an older bug.

The narrowing that mattered came from the symptom itself. In `rlDrawRenderBatch`, `RL_LINES` and `RL_TRIANGLES` go through `glDrawArrays` while `RL_QUADS` goes through `glDrawElements` — and raygui's panels, *all* text and the viewport blit are quads. But the sharper reading is the right one: **lines are not subject to the face-cull test at all.** Everything that vanished was a triangle.

Then a wrong fix made it obvious. Chasing a separate observation — that the composite pipeline declares `CULL_MODE_NONE` and so ends the frame with culling off, breaking the "culling happens to match rlgl's defaults" note in Invariants — `rlEnableBackfaceCulling()` was added to `yieldToRaylib`. That made the editor **and the game** white on every frame from startup. Which named the cause: `RasterizerStateDesc::FrontCounterClockwise` defaults to false, the composite pipeline never sets it, `GLContextState::SetFrontFace` maps it straight through (`FrontCounterClockwise ? GL_CCW : GL_CW`, no inversion — checked, because a Y-flip would have been a plausible reason for one), and raylib winds counter-clockwise. Turning the cull test on against `GL_CW` culls every triangle raylib draws.

Restoring the winding alongside the cull test fixed the regression **and the original bug**, which is the part worth keeping: the gizmo calls `rlEnableBackfaceCulling()` (`raygizmo.c:404`, `:492`) and only draws once a node is selected, so it was turning the cull test on against a winding that had been wrong all along. One state restore, two symptoms.

Verified by running the editor unattended and driving the click through `SetCursorPos`/`mouse_event` rather than asking for it: node selected in the tree, inspector populated with Transform and MeshRenderer, gizmo axes drawn in the viewport, 216 distinct colours sampled across the frame against 201 before the click. The colour histogram is the cheap check here — a culled UI collapses to two or three colours.

⚠️ **A related fact fell out and is now in Invariants: `InvalidateState()` does not reset the rasterizer-state cache at all.** `GLContextState::Invalidate` clears program, pipeline, VAO, FBO and the bound-resource maps; `m_RSState` is not touched. So cull mode and front face survive the call *and* the frame, and Diligent will skip a call it believes is redundant even after rlgl has changed that state behind its back.

**Verification is by identity, which is the useful part.** The test scene's `_tonemap` is `Linear`/1/1 and its `_finalColor` is 1/1/1, so the entire chain — decode, HDR, exposure, passthrough, grading, encode — is mathematically the identity. The criterion is therefore *no visual change at all*, and specifically the viewport background still reading exactly `(66, 78, 96)`. Same shape of check as the raylib 6.0 bump: a change this size earns its keep by what it unblocks, not by what it alters.
