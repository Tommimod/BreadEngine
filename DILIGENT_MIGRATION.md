# Migration plan: r3d → DiligentEngine

## Current Progress

> Update this section as work lands. One line per completed step; move the active phase's checklist items from "planned" to "done" as you go.

**Status:** Phase 0 — Spike decisions — **COMPLETE**. Ready to start Phase 1 (build system integration) whenever you want to proceed.

**Environment setup done alongside Phase 0**: Vulkan SDK 1.4.357.0 installed to `C:/VulkanSDK/1.4.357.0` (official LunarG installer, required admin elevation via UAC). Confirmed `VK_LAYER_KHRONOS_validation` is now available and detected by the Diligent Vulkan spike (previously logged as "not available" before install) — validation layer is ready for use once real shader/pipeline work starts in Phase 3.

- [x] 0.1 Windowing coexistence spike (raylib GL context + Diligent device on same HWND) — **PASSED**, see findings below
- [x] 0.2 Descope editor UI chrome decision — **confirmed**: keep raylib/raygui/rlgl permanently for BreadEditor's 2D UI chrome (this was already the plan's stated recommendation, approved when the plan itself was approved; only the 3D viewport content and gizmos move to Diligent)
- [x] 0.3 Viewport interop spike (Diligent GL-backend sharing vs CPU readback blit) — **PASSED**, GL-sharing approach confirmed viable, see findings below
- [x] 0.4 GLTF_PBR_Renderer adoption spike (drive directly vs reference-only) — **DECIDED**: use `PBR_Renderer` directly, `GLTF_PBR_Renderer` reference-only, see findings below

**Spike 0.1 results** (throwaway code in `spikes/diligent-poc/`, DiligentCore cloned there as a plain non-submodule clone for the spike only — not wired into the real build yet, that's Phase 1):
- Cloned DiligentCore, configured with the project's actual toolchain (clang++ via MSYS2 `C:/msys64/clang64/bin`, Ninja). Built `Diligent-GraphicsEngineOpenGL-static` and `Diligent-GraphicsEngineVk-static` successfully (430/430, exit 0) — **DiligentCore compiles fine with this toolchain for GL and Vulkan**, no need to introduce MSVC just for a build to succeed on those two backends.
- **D3D11/D3D12 are NOT available with this toolchain.** DiligentCore's CMake does `try_compile` checks for `d3d11.h`/`d3d12.h`/`atlbase.h`; the first two are found but `atlbase.h` (ATL) is not, because MSYS2's clang64 uses MinGW-w64 headers, which don't ship ATL. Result: `D3D11_SUPPORTED=FALSE`, `D3D12_SUPPORTED=FALSE`, `GL_SUPPORTED=TRUE`, `VULKAN_SUPPORTED=TRUE`. **Open decision, not yet made:** accept Vulkan+GL only (covers PC+SteamDeck, the stated goal) and drop D3D11/D3D12 from scope, or set up a separate MSVC/clang-cl toolchain with real Windows SDK+ATL later specifically to unlock D3D backends. Revisit when/if a D3D-specific need shows up (e.g. PIX profiling, Xbox-adjacent work) — not needed for the current goal.
- **Built and ran a real coexistence test**: `spikes/diligent-poc/main.cpp` creates a raylib window (`InitWindow`, GLFW/GL backend), passes raylib's `GetWindowHandle()` into `EngineFactoryVk`'s `CreateDeviceAndContextsVk`/`CreateSwapChainVk` (`Win32NativeWindow{hwnd}`), then runs 60 frames doing **both** raylib's `BeginDrawing/EndDrawing` (GL) **and** Diligent's `SetRenderTargets/ClearRenderTarget/Present` (Vulkan) on the same HWND every frame. Ran clean, no crash, no conflict. **This confirms the plan's Phase 0.1 hypothesis: raylib can keep owning the window/input, Diligent can own the swap chain, no need to move off raylib for windowing.**
- Side findings to remember for Phase 1 (build system): the executable needs `libc++.dll`/`libunwind.dll` alongside it or `clang64/bin` on `PATH` at runtime (MSYS2 clang64 links libc++ by default) — the main project presumably already handles this via a system-wide PATH entry, but it's worth an explicit post-build copy step once Diligent is wired into the real CMake, for reproducibility on a clean machine. Also: the real Vulkan SDK is **not actually installed** on this machine despite a stale PATH entry (`C:/VulkanSDK/1.4.321.1/Bin` referenced but doesn't exist) — DiligentCore built and ran fine against its bundled Vulkan-Headers/volk without it, but the validation layer (`VK_LAYER_KHRONOS_validation`) was unavailable at runtime, which you'll want installed once doing real shader/pipeline debugging in Phase 3+.

**Spike 0.3 results** (`spikes/diligent-poc/main_gl_interop.cpp`): tested whether Diligent's OpenGL backend can attach to raylib's already-created GL context and produce a texture raylib can blit directly (zero-copy), matching `viewportWindow.cpp`'s existing `R3D_SetResolution`/`DrawTexturePro` pattern.
- Confirmed `IEngineFactoryOpenGL::AttachToActiveGLContext` is the right call — but only when `EngineGLCreateInfo::Window.hWnd` is left **null**. A non-null handle routes Diligent's Win32 GL backend (`GLContextWindows.cpp`) through the "create our own context" path, which calls `SetPixelFormat` on the window's HDC — and Windows forbids setting a pixel format twice on the same HDC, so it throws (`Failed to set Pixel Format`) since raylib/GLFW already set one. With `hWnd` null, it takes the actual attach path (`wglGetCurrentContext()` + GLEW init against it), which worked immediately.
- Created a 256x256 `ITexture` (`BIND_RENDER_TARGET | BIND_SHADER_RESOURCE`), read its native GL handle via `ITexture::GetNativeHandle()` (returns the GL texture name directly, no need to `QueryInterface` for `ITextureGL`), and wrapped it as a raylib `Texture2D` by hand (`id`/`width`/`height`/`mipmaps`/`format` fields) — `DrawTexturePro` accepted it and rendered the Diligent-cleared texture correctly. Confirms the zero-copy path `viewportWindow.cpp` will need works.
- **Real gotcha found (not hypothetical)**: after Diligent's `SetRenderTargets`+`ClearRenderTarget` bind its own FBO on the shared context, raylib's rlgl has no idea the framebuffer binding changed underneath it — the first attempt produced a fully black window with raylib's draws silently landing inside Diligent's 256x256 offscreen texture instead of the real backbuffer (confirmed by screenshot: a black frame with only a ~256x256 correctly-rendered patch in the corner, sized exactly like the RT). Fixed by explicitly calling `pContext->SetRenderTargets(0, nullptr, nullptr, ...)` + `pContext->InvalidateState()` + `pContext->Flush()` **before** returning control to raylib each frame. **Carry this into Phase 8**: any code that lets raylib and Diligent share a GL context must bracket Diligent's rendering with this unbind/invalidate/flush sequence, or raylib's subsequent draws will silently go to the wrong target.
- Verified visually via screenshot (`gl_interop_result.png`), not just "didn't crash" — confirmed the orange Diligent-rendered square actually composites correctly into the raylib-drawn frame at the expected screen position.

**Spike 0.4 results** (read-only source inspection of `spikes/diligent-poc/DiligentFX/PBR/`, no build needed for this one): DiligentFX splits PBR support into two layers, and this split matters a lot for how much of Phase 4/5 can lean on it:
- `PBR_Renderer` (`PBR/interface/PBR_Renderer.hpp`) is the actual format-agnostic PBR pipeline: PSO management (`GetPSO`/`CreatePSO` returning raw `IPipelineState*`), a generic vertex attribute layout (`VERTEX_ATTRIB_ID_POSITION/NORMAL/TEXCOORD0/TEXCOORD1/JOINTS/WEIGHTS/COLOR/TANGENT`), and a generic material texture slot layout (`TEXTURE_ATTRIB_ID_BASE_COLOR/NORMAL/METALLIC/ROUGHNESS/OCCLUSION/EMISSIVE` plus clear-coat/sheen/anisotropy/iridescence/transmission for later). None of this requires glTF loading — it operates on `IPipelineState`/`IShaderResourceBinding`/vertex-buffer level.
- `GLTF_PBR_Renderer` (`public GLTF_PBR_Renderer : public PBR_Renderer`) adds a convenience layer on top whose `Render()` entry point takes a `GLTF::Model&`/`GLTF::ModelTransforms` directly — this part **is** glTF-specific and not reusable without adopting glTF's scene-graph loading conventions.
- **Decision**: drive `PBR_Renderer` directly with BreadEngine's own mesh/material data (matching its vertex/texture attribute IDs, since `meshAsset`'s existing texture-slot auto-wiring already approximates the glTF metallic-roughness convention per the original migration plan) rather than adopting `GLTF::Model`/`GLTF_PBR_Renderer::Render()`. Use `GLTF_PBR_Renderer`'s implementation only as a reference for how it drives the base class (PSO selection logic, resource binding order), not as a dependency. This de-risks Phase 4/5: BreadEngine's asset pipeline does not need to be reshaped around glTF's loader to get DiligentFX's PBR shading.

**Known environment facts relevant to Phase 0** (discovered during spike setup, not in original plan):
- Project builds with **clang++ via MSYS2 (`C:/msys64/clang64/bin/clang++.exe`) + Ninja** on Windows, not MSVC. DiligentEngine's Windows build/samples are primarily documented/tested against MSVC; clang/MSYS2 compatibility is now verified for GL+Vulkan (see above) — D3D11/D3D12 is the one gap.

_(Phases 1-11 below unchanged from plan.)_

---

## Context

BreadEngine's rendering is currently 100% OpenGL, locked in via r3d (a PBR renderer built on raylib's rlgl abstraction). There is no path from here to Vulkan/D3D, which blocks the long-term goal of a future-proof renderer for shipping a real game (horror/adventure) on PC and eventually SteamDeck/consoles. DiligentEngine has been chosen as the target RHI (D3D11/D3D12/Vulkan/Metal/GL, plus a GLTF PBR Renderer + deferred shading + SSAO/Bloom in DiligentFX/DiligentSamples). This plan sequences that migration so BreadEngine stays buildable and runnable at every step (strangler-fig, not big-bang), given this is solo part-time hobby work with eventual AI-agent assistance on implementation, while architectural decisions stay with you.

Verified against the current codebase (not assumed): `Light::lightType` is a raw `R3D_LightType` serialized directly via `INSPECT_FIELD(lightType)` ([light.h:8,26](modules/engine/component/light.h)) — a real migration risk for existing scene files, not hypothetical. `games/example_game/main.cpp:35` calls `R3D_Begin`/`R3D_End` directly, a second call site alongside the editor's. The editor's entire 2D UI toolkit (11+ files under `modules/editor/uitoolkit/`) renders through **raygui**, itself built on raylib/rlgl — this is a separate rlgl consumer from the 3D viewport, and matters for scoping Phase 0.

## Phase 0 — Spike decisions (do first, throwaway code, days–2wk)

Resolve empirically outside `modules/`, before touching real code:

1. **Windowing stays raylib.** `raylib`'s `GetWindowHandle()` gives the native handle (`HWND` etc.) Diligent's `NativeWindow` needs for swap-chain creation; `raymath.h`/`rcamera.h` are header-only with no runtime link, so existing `Camera3D` math survives untouched. Spike that a GL context (raylib's `InitWindow`) and a Diligent D3D/Vulkan device can coexist on the same HWND without conflict — expected to be fine (different APIs, no shared pixel-format constraint) but must be proven, not assumed. Fall back to GLFW only if this proves flaky (costs a rewrite of `Engine::Input`).
2. **Descope the editor's 2D UI chrome from this migration.** Keep raylib+raygui+rlgl permanently for `BreadEditor`'s buttons/panels/inspector — the editor never ships, only the runtime/game does. Only the 3D viewport content and gizmos move to Diligent. This avoids an unplanned rewrite of the 26-file UI toolkit that has nothing to do with the actual goal (a future-proof shipped-game renderer).
3. **Viewport interop:** spike running Diligent's own **OpenGL backend** for the editor process so the rendered scene is a plain `GLuint`, wrappable into a raylib `Texture2D` for the existing `DrawTexturePro` blit in `viewportWindow.cpp` — reuses today's code path almost verbatim. Fall back to CPU-readback blit only if GL-context-sharing is fragile (simpler, slower, acceptable for an editor preview pane, irrelevant to the shipped game which owns its own swap chain directly).
4. **GLTF_PBR_Renderer adoption vs. reference-only:** spike whether DiligentFX's `GLTF_PBR_Renderer` can be driven directly from BreadEngine's own mesh/material data, or whether it's more pragmatic to use it purely as a reference for shader/PSO structure and write a slimmer engine-owned equivalent. Decides how much Phase 4/5 can lean on it directly.

## Phases 1–9 — Backend cutover (feature parity with today's R3D rendering)

Each phase keeps the tree buildable; Phases 2–8 keep R3D alive behind the new seam via a `BREAD_RENDER_BACKEND=R3D|Diligent` switch, so regressions are checked by screenshot-diffing the same scene under both backends — this is the QA method that substitutes for not having a rendering engineer's eye.

1. **Build system.** Add DiligentCore/DiligentTools/DiligentFX as git submodules (Diligent's documented approach — its CMake uses custom macros that plain `add_subdirectory` handles better than `FetchContent`), wired in alongside the existing vendored raylib/r3d/assimp blocks in root `CMakeLists.txt` — remove nothing yet. Collapse the three duplicated DLL-copy blocks (root/editor/example_game) into one reusable CMake function while here.
2. **Insert the seam.** New `modules/engine/rendering/IRenderer.h` + opaque handle types (`MeshHandle`, `MaterialHandle`, `TextureHandle`, `LightHandle` — plain index/generation structs, not per-object virtual dispatch, since only one backend is active at a time). Move every R3D call site (`meshRendererSystem.cpp`, `lightSystem.cpp`, `globalLightSystem.cpp`, `textureAsset.cpp`, `material.cpp`, `meshRenderer.cpp`, `spriteRenderer.cpp`, `editor.cpp`, `example_game/main.cpp`) into one `R3DRenderer : IRenderer`. Components (`MeshRenderer`, `SpriteRenderer`, `Light`, `Material`, `TextureAsset`, `Camera`) hold handles, not raw R3D types. **Before touching `Light`/`Material`: confirm whether `.yaml` scene files bake `R3D_LightType`'s numeric enum value** (check `BaseYamlConfig`/inspector serialization) — if so, either keep the enum's underlying values stable across the swap or write a one-time migration for existing scene assets. This phase should be a pure refactor with zero visual change — validate with a before/after screenshot diff.
3. **DiligentRenderer MVP.** Forward-shaded (not deferred) single PBR pipeline: PSO + SRB, one directional light, no shadows/IBL/post yet. Wire the existing `Camera3D`/`rcamera.h` math into Diligent's per-frame constant buffer (matrix-convention conversion is the only new math needed). Milestone: a textured lit cube renders equivalently via both backends. This is where the "not a rendering engineer" learning curve is steepest (PSOs/SRBs are conceptually unlike raylib's globals-based rlgl API) — budget accordingly, and treat DiligentSamples Tutorials 01–05 as the reference path.
4. **Materials & textures cutover.** Replace `TextureAsset`'s raw `Image`/`Texture2D`/`R3D_Cubemap` members with `TextureHandle` (DiligentTools' `TextureLoader` loads PNG/JPG/HDR/KTX/DDS straight into an `ITexture`, likely a cleaner reuse point than raylib decode + manual upload — confirm in Phase 0.4). Replace `Material`'s `R3D_Material` wrapping with an engine-native albedo/normal/ORM/emission struct shaped to glTF's metallic-roughness convention, since `meshAsset`'s existing texture-slot auto-wiring already approximates it.
5. **Mesh primitives, MeshRenderer, SpriteRenderer.** Reimplement the ~10 `R3D_GenMesh*` generators (cube/sphere/hemisphere/cylinder/capsule/plane/quad/slope/torus/poly) as engine-native geometry producing Diligent vertex/index buffers — genuinely new code (no Diligent equivalent exists), but standard parametric math, good AI-assist candidate. Port `meshAsset`'s assimp import to build an engine-native cached mesh format directly (today assimp/R3D only transiently enumerate material count, geometry isn't cached) — this also lets Phase 9 scope assimp to import-time/editor-only, dropping it from the shipped game's runtime deps entirely.
6. **Lights.** Replace `lightSystem.cpp`'s full R3D light diff-sync with `IRenderer` calls. Use DiligentFX's `ShadowMapManager` (CSM) for directional shadows first — closest match to an existing DiligentFX component. Point/spot shadows are custom follow-up with less direct reuse; given the horror/adventure genre, flag this as a genre-relevant priority call for you, not something to auto-deprioritize.
7. **Environment / post-processing / skybox.** Cleanest phase given the existing abstraction: only the `toNative`/`fromNative` bodies in `configs/light/*.h` (background/ambient/ssao/ssil/ssgi/ssr/bloom/fog/dof/tonemap/color) need retargeting to DiligentFX PostFX settings — `globalLightSystem.cpp` call sites shouldn't need to change. `R3D_GenProceduralSky` has no Diligent equivalent (it's an analytic sky shader, not an HDRI) — port a known public model (Preetham or Hosek-Wilkie) as an HLSL shader. Close the previously-stubbed Cubemap/Custom skybox modes now that Phase 4 gives a real texture/cubemap pipeline to hang them on.
8. **Editor viewport integration.** Implement whatever Phase 0.3 spiked (GL-sharing or readback) so `viewportWindow.cpp`'s `R3D_SetResolution`/`DrawTexturePro` pattern becomes Diligent-scene-into-raygui-panel. Leave `raygizmo.c` (confirmed rlgl-based: `rlBegin`/`rlVertex3f`/`rlGetMatrixModelview`) as a raylib/rlgl overlay drawn after the Diligent blit into the same panel — continuing the existing compositing pattern rather than porting a third-party gizmo lib.
9. **Remove R3D/rlgl from engine+game.** Delete the R3D `IRenderer` backend, drop r3d/rlgl includes from engine/game code and `R3D_DLL`/`ASSIMP_DLL`(if scoped to editor-only) from CMake. Pure deletion by construction, since Phases 2–8 already proved behavioral parity. Editor keeps raylib/raygui/rlgl for its own chrome per Phase 0.2.

## Phases 10–11 — Greenfield (no porting burden, new subsystems)

10. **Skeletal/bone animation.** No existing BreadEngine code to port (confirmed zero hits for Animation/Skeleton/Bone). Use DiligentFX's `GLTF_PBR_Renderer`/DiligentSamples' GLTF viewer skinning support as the reference (joint indices/weights, joint matrix buffers, GPU skinning) rather than inventing it from scratch. Extend the Phase 5 assimp importer to bake skeleton hierarchy + bind pose + clips. This is a full subsystem (importer + runtime pose blending + GPU skinning + any editor preview tooling) — the GPU-skinning mechanics are well-referenced (good AI-assist fit), but animation runtime design (blending, state machines) is new architecture only you should own.
11. **Shader scripting/authoring.** Fills the two currently-dead placeholder fields (`Material::_shaderPath`, `GlobalLightSettings::_skyboxShaderPath`). Diligent shaders are HLSL, cross-compiled via DXC to SPIRV/GLSL/MSL, with `IShaderSourceInputStreamFactory` for `#include` resolution — a natural foundation. Compile user HLSL fragments into fixed material "shader slots" at runtime; hot-reload by watching file mtime and recreating the affected `IPipelineState` (cheap in Diligent, a common pattern). Scope is your call: a fixed-uniform HLSL-snippet system vs. a node-graph editor are very different sizes — the compile/hot-reload mechanics are good AI-assist material, but the authoring UX is a first-party design decision.

## Cross-cutting notes

- **Genre priority:** for horror/adventure, shadow quality (point/spot, not just directional) and post-processing (bloom/DOF/fog/color grading) likely matter more to final look than deferred shading or high light counts. Treat deferred shading (via Diligent's render-passes/subpass pattern) as an optional upgrade after Phase 9, not a Phase 3 requirement — don't conflate "reach parity with today's R3D rendering" with "modernize further."
- **Assimp** should end up import-time/editor-only after Phase 5, removing it from the shipped game's runtime dependencies — a free win for the SteamDeck-shipping goal.

## Rough effort (solo, part-time, order of magnitude)

Phase 0: days–2wk · 1: 2–4wk · 2: 3–6wk · 3: 4–8wk · 4: 2–4wk · 5: 4–6wk · 6: 4–8wk · 7: 2–3wk · 8: 2–4wk · 9: <1wk · 10: 2–4mo · 11: 1–4mo (scope-dependent). Phases 1–9 (full parity cutover) land around **6–12 months** part-time; Phases 10–11 are additional and can interleave once Phase 5's mesh pipeline exists.

AI-agent assistance fits best where there's dense reference material and no visual judgment call: PSO/SRB boilerplate, mesh-generator math, assimp import patterns, GPU-skinning mechanics, shader hot-reload plumbing. Keep for yourself: visual correctness review at every phase gate, the handle/interface boundary design in Phase 2, resource-lifetime/sync decisions, and any UX/authoring-surface design in Phases 10–11.

## Verification

- After Phase 2: load an existing scene under both `BREAD_RENDER_BACKEND=R3D` and confirm zero behavior change (same screenshot, same serialized files load without migration).
- After each of Phases 3–7: screenshot-diff the same test scene under R3D vs. Diligent backends; visually confirm lighting/shadows/post-fx look equivalent before moving on.
- After Phase 9: confirm editor and `example_game` both build and run with R3D fully removed from engine/game link targets, editor still runs its raylib/raygui chrome.
- Phases 10–11: no direct R3D comparison possible (greenfield) — verify against known-good reference output (e.g. a public glTF skinned sample model) instead.

### Critical files
- `CMakeLists.txt`, `modules/engine/CMakeLists.txt`, `modules/editor/CMakeLists.txt`, `games/example_game/CMakeLists.txt`
- `modules/engine/engine.h` / `engine.cpp`
- `modules/engine/component/meshRenderer.h/.cpp`, `spriteRenderer.h/.cpp`, `light.h`, `camera.h`
- `modules/engine/data/material.h/.cpp`
- `modules/engine/configs/assets/textureAsset.h/.cpp`, `meshAsset.h/.cpp`
- `modules/engine/configs/light/*.h/.cpp`, `globalLightSettings.h`
- `modules/engine/systems/meshRendererSystem.cpp`, `lightSystem.cpp`, `globalLightSystem.cpp`, `cameraSystem.cpp`
- `modules/editor/editor.cpp`, `modules/editor/windows/viewportWindow.cpp`, `modules/editor/systems/gizmoSystem.cpp`, `lib/editor/raygizmo.c`
- `games/example_game/main.cpp`
